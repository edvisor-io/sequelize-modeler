<%_
const exceptions = {
  // If you would like you model names to be different than the table name for a select few table,
  // you could map out those exceptions here
  'SomeWeirdTableName': 'BetterModelName',
}
-%>
<%- modelName && exceptions[modelName] || modelName %>