interface <%= attributesInterfaceName %><%
  if (hasCreated || hasModified) { %> extends <%
    if (hasCreated) { %>CreateableModel<% }
    if (hasCreated && hasModified) { %>, <% }
    if (hasModified) { %>ModifiableModel<% }
  } %> {
<%_ table.columns.forEach((column, columnName) => { -%>
  <%= changeCase.camelCase(columnName) %>: <%= column.tsType %>
<%_ }) -%>
}