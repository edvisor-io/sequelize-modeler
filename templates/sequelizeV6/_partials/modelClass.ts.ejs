<%_
const instanceName = changeCase.camelCase(table.name)
-%>
class <%= modelName %> extends Model<
  <%= attributesInterfaceName %>,
  <%= createAttributeInterfaceName %>
> implements <%= attributesInterfaceName %> {
<%_ table.columns.forEach((column, columnName) => { -%>
  public <%= changeCase.camelCase(columnName) %>!: <%- include('tsType.ejs', { column }) %>
<%_ }) -%>

}