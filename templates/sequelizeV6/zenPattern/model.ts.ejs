<%_
const modelName = include('_partials/modelNames.ejs', {
  modelName: changeCase.pascalCase(table.name),
})

const fileName = changeCase.paramCase(modelName)
setOutputFileName(`server/sequelize/models/${fileName}.ts`)
-%>
import { ModelCtor } from 'sequelize'

import { <%= modelName %>Schema } from '../schema/<%= fileName %>'

export default class <%= modelName %> extends <%= modelName %>Schema {
}

export type <%= modelName %>Ctor = ModelCtor<<%= modelName %>> & {
  // static methods go here
}