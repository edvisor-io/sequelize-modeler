<%_
const instanceName = changeCase.camelCase(table.name)
const attributesToSetUndefined = {}
table.columns.forEach((column, columnName) => {
  if (column.primaryKey || _.includes(['created', 'modified'], columnName)) {
    attributesToSetUndefined[columnName] = undefined
  }
})
-%>
class <%= modelName %> extends Model<
  <%= attributesInterfaceName %>,
  <%= createAttributeInterfaceName %>
> implements <%= attributesInterfaceName %> {
<%_ table.columns.forEach((column, columnName) => { -%>
  public <%= changeCase.camelCase(columnName) %>!: <%= column.tsType %>
<%_ }) -%>

}

export type <%= modelName %>Model = ModelCtor<<%= modelName %>> & {
  // static methods go here
}